#include <stdio.h>
#include <stdlib.h>

typedef struct _queue
{
    int arr[10];
    int first;
}
queue;

int main(void)
{
    queue myQueue;
}

void add(queue* q, int data) {}

void pop(queue* q) {}
