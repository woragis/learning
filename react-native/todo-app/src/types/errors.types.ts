export enum UserResponseErrors {
  'server error' = 0,
  'did not receive name input' = 1,
  'did not receive email input' = 2,
  'did not receive password input' = 3,
  'email not found' = 4,
  'password wrong' = 5,
}

export enum TodosResponseErrors {
  'server error' = 0,
  'did not receive author input' = 1,
  'did not receive title input' = 2,
  'did not receive description input' = 3,
}
